1. Named Parameters using Destructuring Objects

// arguments must be in specific order
function meal(protein, card, veggie){}
meal('meat', 'rice', 'vegetable')

// arguments can be in any order
function meal({protein, card, veggie}){}
meal({carb: 'rice', veiggie: 'vegetable', protein: 'meat'})

Buat arrange parameters

2. Combine Multiple Arrays using Spread
let veggie = ['tomata', 'avocado']
let meat = ['meat']

//Old way
let sandwich = veggie.concat(meat, 'bread')

//ES6 way
let sandwich = [...veggie, ...meat, 'bread']

3. Split String using ES6 Spread

const pizza = 'pizza'

//Old Way
const slicedPizza = pizza.split('')

// ES6 Way
const slicedPizza = [...pizza]

//Result
// [ 'p', 'i', 'z', 'z', 'a' ]

4. Remove Array Duplicates using ES6 Set

const duplicates = [1,2,3,4,4,1]

const uniques = Array.from(new Set(duplicates))
uniques

5. ES6 way of creating object with dynammic keys

var key = 'key';

//Old Way
var lock = {}
lock[key] = 'unlock'

//New Way
car lock = {
  [key]: 'unlock'
}

6. ES6's shorthand for defining methods in Objects

const Rain = {
 // bringUmbrella: function() {}
 bringUmbrella(){
   return
 }
}

7. convert to true array 

const nodeList = document.querySelectorAll('ul li');

Array.from(nodelist);
[...nodeList]

8. LowerCase
const original = 'WeB BasIcS 102';
const ower = original.toLowerCase();

can't work with null, undefined, array and number

9. While loop

let money = 10;

while(money){
  console.log('belanja cuy')
  money--
}

10. You can use autofocus on html so after the web full render he can fill the form

11. Reverse String

function reverseString(str) {
  return str
  .split('')
  .reverse('')
  .join('')
}

12. Dynamic Property name with ES6

let cake = 'cake'basic

// Old way requires 2 steps
let pan = {};
pan[cake] = 'cake';

//New Way
let pan = {
  [cake]: 'cake'
}

13. ES6 Way to Clone an Array
const sheeps = ['Kambing', 'Kambing', 'Kambing']

//Old Way
const clone Sheeps = sheeps.slice()

//ES6 Way
const cloneSheeps = [...sheeps]